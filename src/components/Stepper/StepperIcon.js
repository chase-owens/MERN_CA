import React from 'react';

const StepperIcon = ({ content }) => {
  return (
    <p
      style={{
        textAlign: 'center',
        height: '100%',
        lineHeight: '1px',
        color: '#fff'
      }}
    >
      {content}
    </p>
  );
};

export default StepperIcon;
